<h2>All Libraries</h2>

<ul>
  <% @libraries.each do |mix| %>
    <li>
      <%= link_to mix.name, library_path(mix) %>
      <% if @current_user.present? && mix.user_id == @current_user.id %>
        (<%= link_to 'Edit', edit_library_path(mix.id) %>)
      <% end %>
    </li>
  <% end %>
</ul>