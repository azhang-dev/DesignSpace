<h2>Project Index</h2>

<ul>
    <% @current_user.projects.each do |project| %>
    <li>
    <%= link_to image_tag(project.image_url, class:'index_image'), project_path(project.id) %> 
    </li>

    <% end %>
</ul>