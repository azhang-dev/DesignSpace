
<ul>
    <% @projects.each do |project| %>


    <li>
        <div >
        <%= link_to cl_image_tag(project.image, class:'index_image'), project_path(project.id) %> 

        <p id ='index_title'><%= project.title %></p>
        </div>
    </li>

    
    <% end %>
</ul>